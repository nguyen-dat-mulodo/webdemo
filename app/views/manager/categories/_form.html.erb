<%= form_for(@category, url: manager_category_path, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @category.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h4>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :cat_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :cat_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cat_name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :cat_name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cat_content, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :cat_content, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
